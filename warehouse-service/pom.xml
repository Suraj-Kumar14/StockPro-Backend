<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.5</version>
		<relativePath/>
	</parent>

	<groupId>com.stockpro</groupId>
	<artifactId>warehouse-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>warehouse-service</name>
	<description>Warehouse Service</description>

	<properties>
		<java.version>17</java.version>

		<!-- Correct Spring Cloud version for Spring Boot 3.2.5 -->
		<spring-cloud.version>2023.0.1</spring-cloud.version>

		<springdoc.version>2.3.0</springdoc.version>
		<jacoco.version>0.8.14</jacoco.version>
		<sonar.maven.plugin.version>5.6.0.6792</sonar.maven.plugin.version>

		<!-- SonarQube -->
		<sonar.projectKey>stockpro-warehouse-service</sonar.projectKey>
		<sonar.projectName> Warehouse Service</sonar.projectName>
		<sonar.host.url>http://localhost:9000</sonar.host.url>
		<sonar.coverage.jacoco.xmlReportPaths>target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>

		<sonar.sources>src/main/java</sonar.sources>
		<sonar.tests>src/test/java</sonar.tests>
		<sonar.test.exclusions>src/test/java/**</sonar.test.exclusions>
		<sonar.cpd.exclusions>src/test/java/**</sonar.cpd.exclusions>

		<sonar.exclusions>
			**/target/**,
			**/.mvn/**
		</sonar.exclusions>

		<sonar.coverage.exclusions>
			src/main/java/com/stockpro/warehouseservice/WarehouseServiceApplication.java,
			src/main/java/com/stockpro/warehouseservice/aspect/**,
			src/main/java/com/stockpro/warehouseservice/client/**,
			src/main/java/com/stockpro/warehouseservice/config/**,
			src/main/java/com/stockpro/warehouseservice/dto/**,
			src/main/java/com/stockpro/warehouseservice/entity/**,
			src/main/java/com/stockpro/warehouseservice/enums/**,
			src/main/java/com/stockpro/warehouseservice/events/**,
			src/main/java/com/stockpro/warehouseservice/exception/**,
			src/main/java/com/stockpro/warehouseservice/rabbitmq/**,
			src/main/java/com/stockpro/warehouseservice/security/**,
			src/main/java/com/stockpro/warehouseservice/service/RabbitMQWarehouseEventPublisher.java,
			src/main/java/com/stockpro/warehouseservice/service/WarehouseEventPublisher.java
		</sonar.coverage.exclusions>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-amqp</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aop</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.11.5</version>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>

			<!-- Spring Boot build plugin -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>

			<!-- JaCoCo coverage plugin -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>

					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<excludes>
								<exclude>com/stockpro/warehouseservice/WarehouseServiceApplication*</exclude>
								<exclude>com/stockpro/warehouseservice/aspect/**</exclude>
								<exclude>com/stockpro/warehouseservice/client/**</exclude>
								<exclude>com/stockpro/warehouseservice/config/**</exclude>
								<exclude>com/stockpro/warehouseservice/dto/**</exclude>
								<exclude>com/stockpro/warehouseservice/entity/**</exclude>
								<exclude>com/stockpro/warehouseservice/enums/**</exclude>
								<exclude>com/stockpro/warehouseservice/events/**</exclude>
								<exclude>com/stockpro/warehouseservice/exception/**</exclude>
								<exclude>com/stockpro/warehouseservice/rabbitmq/**</exclude>
								<exclude>com/stockpro/warehouseservice/security/**</exclude>
								<exclude>com/stockpro/warehouseservice/service/RabbitMQWarehouseEventPublisher*</exclude>
								<exclude>com/stockpro/warehouseservice/service/WarehouseEventPublisher*</exclude>
							</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- SonarQube Maven scanner -->
			<plugin>
				<groupId>org.sonarsource.scanner.maven</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
				<version>${sonar.maven.plugin.version}</version>
			</plugin>

		</plugins>
	</build>

</project>
