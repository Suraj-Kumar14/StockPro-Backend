server:
  port: 8081
  forward-headers-strategy: framework

spring:
  application:
    name: AUTH-SERVICE

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:surajkr1122003@gmail.com}
    password: ${MAIL_PASSWORD:kyydpmjoefdmacdh}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:72085009922-uj1d6o04b2920a0gabn54ln2aq2ie8jt.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            redirect-uri: http://localhost:8080/login/oauth2/code/google
            scope:
              - openid
              - profile
              - email

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Suraj@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false
    hostname: localhost

# JWT config
jwt:
  secret: my-secret-key-ayush-chouhan-stockpro-secret-key-2024
  expiration: 28800000
  refresh-expiration: 604800000

logging:
  level:
    com.stockpro: DEBUG
  file:
    name: logs/auth-service.log

app:
  frontend-url: ${FRONTEND_URL:http://localhost:4200}
  otp:
    expiry-minutes: ${OTP_EXPIRY_MINUTES:10}
  mail:
    from: ${MAIL_FROM:surajkr1122003@gmail.com}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true
